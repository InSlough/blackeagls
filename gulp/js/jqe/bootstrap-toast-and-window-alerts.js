// !

// $(".toast").toast();
// window.alert = function (T) {
//   // console.log("alert:", T);
//   var newID = randID();
//   var htmlAlertTemplate = `<div id="toast${newID}" class="toast d-flex align-items-center text-white bg-primary border-0" role="alert" aria-live="assertive" aria-atomic="true"><div class="toast-body">${T}</div><button type="button" class="btn-close btn-close-white ml-auto mr-2" data-dismiss="toast" aria-label="Close"></button></div>`;
//   $("#toast-wrapper").append(htmlAlertTemplate);
//   let nt = $("body").find("#toast" + newID);
//   nt.on("hidden.bs.toast", () => nt.remove());
//   nt.toast("show");
// };
